Searches for a given `pathname` found on an `image`.
Prints some metadata, and the block numbers of direct, indirect and doubly indirect blocks of the `image`.

Run: 
```
sh mk
./a.out <image> <pathname>
```
