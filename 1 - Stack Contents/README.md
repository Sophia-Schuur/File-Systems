Uses a custom `myprintf()` to echo `argc`, `argv` and `env` values, 
prints the addresses of some local variables, the stack frame link list, and the stack contents from `p` to the frame of `main()`.

Run: `sh mk`
